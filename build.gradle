plugins {
    id "com.github.hierynomus.license" version "0.11.0"
}

/*excludeFromAll(project, 'javax.servlet', 'servlet-api')*/

defaultTasks 'build'

apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'eclipse'
apply plugin: 'maven'

version='2.2.0'

license {
    header rootProject.file('src/main/license/xebialabs_community.license')
    strictCheck true
}
